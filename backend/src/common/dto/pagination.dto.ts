// SRP: Defines the standard data transfer object for all paginated API requests.
// WHAT: A Zod schema defining optional `page`, `limit`, `sortBy`, and `sortOrder` query parameters.
// WHY: Enforces a consistent and predictable API for all list-based endpoints.